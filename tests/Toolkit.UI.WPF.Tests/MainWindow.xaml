<Window
    x:Class="Toolkit.UI.WPF.Tests.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Toolkit.UI.WPF;assembly=Toolkit.UI.WPF"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Toolkit.UI.WPF.Tests"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:PlaceholderTextBox
            Width="200"
            Height="50"
            Padding="10,0"
            VerticalContentAlignment="Center"
            AcceptsReturn="True"
            Background="Azure"
            BorderBrush="Black"
            BorderThickness="1"
            CaretBrush="Gray"
            CloseButtonForeground="Red"
            FontSize="20"
            MaxLength="10"
            PlaceholderText="Type here!"
            SpellCheckEnable="True" />

        <Button Grid.Row="1" Content="For lost Focus" />
    </Grid>

</Window>
